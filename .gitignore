# ---------- Java / Maven ----------
*.class
*.log
*.ctxt
.mtj.tmp/
target/
!.mvn/wrapper/maven-wrapper.jar

# ---------- IDE ----------
.idea/
.vscode/
*.iml
*.iws
*.ipr
.classpath
.project
.settings/

# ---------- Node / Vite ----------
node_modules/
dist/
.npm/
.yarn/
.pnp.*
*.tsbuildinfo
auto-imports.d.ts
components.d.ts

# ---------- OS ----------
.DS_Store
Thumbs.db

# ---------- Docker / runtime data ----------
docker/data/
docker/.env
docker/config/
backend/data/
data/

# ---------- Claude Code (local) ----------
# skills/ is tracked (shared OpenSpec skills); commands/ and local settings are per-machine.
.claude/commands/
.claude/settings.local.json

# ---------- Generated frontend bundles ----------
# Vite writes hashed assets here on `mvn package`; index.html in git is a
# placeholder used by `mvn spring-boot:run`.
backend/src/main/resources/static/assets/

# ---------- JVM crash dumps ----------
hs_err_pid*
replay_pid*
